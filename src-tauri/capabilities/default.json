{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Moira cockpit window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-spawn"
  ]
}
